import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import reportWebVitals from './reportWebVitals';
import { Button, Typography } from '@mui/material';
import { TextField } from '@mui/material';
import { color } from '@mui/system';
import { brandText, normalText, normalButton } from './SocioStyles';
import { newUserlogin } from './SocioFunctions';
const root = ReactDOM.createRoot(document.getElementById("rootNewUser"));
root.render(
  <div id='container'>
      
  </div>
);
